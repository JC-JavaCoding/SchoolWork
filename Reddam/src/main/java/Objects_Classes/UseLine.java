/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Objects_Classes;

import javax.swing.JOptionPane;

/**
 *
 * @author Jcj
 */
public class UseLine
{
    public static void main(String[] args)
    {
        /*
        Line stripe = new Line();
        stripe.draw();
        stripe.size = 5;
        stripe.draw();
        stripe.pattern = '&';
        stripe.draw();
        
        //draw equals line
        stripe.pattern = '=';
        stripe.size = 80;
        stripe.draw();
        
        //draw descending number asterisks
        stripe.pattern = '*';
        stripe.size = 5;
        stripe.drawDesc();
        
        
        int inSize = Integer.parseInt(JOptionPane.showInputDialog("Enter the length of the object"));
        char character = JOptionPane.showInputDialog("Input a single char character for pattern").charAt(0);
        
        Line obj = new Line();
        obj.size = inSize;
        obj.pattern = character;
        
        obj.draw();
        
        obj.drawVert();
*/
    }
}
